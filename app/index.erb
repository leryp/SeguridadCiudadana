<div data-role="page" data-url="<%= Rho::RhoConfig.start_path %>/">

  <div data-role="header" data-position="inline">
    <h1>SeguridadCiudadana</h1>
    <% if SyncEngine::logged_in > 0 %>
    <a href="<%= url_for :controller => :Settings, :action => :do_sync %>" class="ui-btn-left" data-icon="refresh">
      Sync
    </a>
    <a href="<%= url_for :controller => :Settings, :action => :logout %>" class="ui-btn-right" data-icon="star">
	    Logout
	  </a>
    <% else %>
        <a href="<%= url_for :controller => :Settings, :action => :login %>" class="ui-btn-right" data-icon="star">Login</a>
    <% end %>
  </div>

  <div data-role="content">
    <ul data-role="listview">
      <li><a href="<%= url_for :controller => :Ciudadano %>">Ciudadanos</a></li>
      <li><a href="<%= url_for :controller => :Sector %>">Sectores</a></li>
      <li><a href="<%= url_for :controller => :Ciudadano, :action => :filtro_ciudadanos_form %>">Ciudadanos x Sector</a></li>
      <li><a href="SystemExamples">Ejemplos Sistema</a></li>
      <li><a href="GeoLocalizacion">Geo Localizacion</a></li>
    </ul>
  </div>

</div>
